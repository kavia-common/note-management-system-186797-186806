# note-management-system-186797-186806

This workspace contains the Next.js frontend for Ocean Notes under `nextjs_frontend/`.

- Navigate to `nextjs_frontend` and run `npm install` then `npm run dev` to start the app at http://localhost:3000.
- See `nextjs_frontend/README.md` for environment variable usage and details.